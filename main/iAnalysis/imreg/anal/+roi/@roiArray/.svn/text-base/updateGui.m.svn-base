%
% Updates the gui ; for now blank
%
function updateGui(obj, params)

	% --- tie gui to mouse processor, key stroke processor
	set(obj.guiHandles(3),'KeyPressFcn', {@obj.guiKeyStrokeProcessor});
	set(obj.guiHandles(1),'ButtonDownFcn', {@obj.guiMouseClickProcessor});
