%
% Wrapper for par_execute to compile on cluster since cluster compilation 
%  allows only one input argument.  Calls par_execute with given parpath
%  with persistence DISABLED.
% 
function par_execute_cluster (parpath)
  par_execute(parpath, 0);
